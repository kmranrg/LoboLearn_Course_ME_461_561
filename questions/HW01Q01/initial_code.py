# Complete the function below.  You MUST import either numpy or torch
# somewhere in this file and use it in a meaningful way (e.g., convert
# `age` to a tensor/array or perform a simple calculation).

def evaluate_claim(gender: str, age: int) -> str:
    """
    Parameters
    ----------
    gender : str
        "male" or "female" (case-insensitive)
    age : int
        Customer age

    Returns
    -------
    str
        "Accept", "Reject", or "Need more info"
    """
    # --- your code here ---
    pass
